<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="And.mapper">
	
	<select id="list" resultType="Android_Ser.AndGalleryVO">
	 select * from sell order by id desc
	</select>
	
	<update id="galupdate">
	 update sell set filepath = replace(filepath, '\', '/')
	</update>
		
	<insert id="andInsert">
	 insert into sell (title, price, content, writer, filename, filepath)
	 values (#{title}, #{price}, #{content}, #{writer}, #{filename, jdbcType=VARCHAR}, #{filepath, jdbcType=VARCHAR})
	</insert>
	
</mapper>